from django.urls import path
from API.views import bot

urlpatterns = [
    # path('bot/get_bot_messages/', bot.GetBotMessages.as_view()),
    # path('bot/verify_user/', bot.VerifyUser.as_view()),
    # path('bot/check_user/', bot.CheckUser.as_view())
]
